name: Build & Deploy SUPPORT

on:
  push:
    branches: [main]
    paths:
      - 'services/support/sections/**'
      - '.github/workflows/build-support.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Assemble SUPPORT from independently deployable sections
        run: |
          set -euo pipefail
          out=.github/SUPPORT.md
          banner='<!-- GENERATED ARTIFACT — do not hand-edit. Source of truth: services/support/sections/*.md. Redeployed by .github/workflows/build-support.yml -->'
          tmp="$(mktemp)"
          printf '%s\n\n' "$banner" > "$tmp"
          first=1
          for f in services/support/sections/*.md; do
            if [ "$first" -eq 0 ]; then printf '\n---\n\n' >> "$tmp"; fi
            cat "$f" >> "$tmp"
            first=0
          done
          mv "$tmp" "$out"

      - name: Redeploy artifact if it drifted from source
        run: |
          set -euo pipefail
          git add -f .github/SUPPORT.md
          if git diff --cached --quiet; then
            echo "No drift detected. SUPPORT artifact already in production. 🛰️"
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'chore(support): redeploy generated artifact to production'
          git push origin HEAD:${{ github.ref_name }}

name: Build & Deploy PULL_REQUEST_TEMPLATE

on:
  push:
    branches: [main]
    paths:
      - 'services/pull-request-template/sections/**'
      - '.github/workflows/build-pull-request-template.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Assemble PULL_REQUEST_TEMPLATE from independently deployable sections
        run: |
          set -euo pipefail
          out=.github/PULL_REQUEST_TEMPLATE.md
          banner='<!-- GENERATED ARTIFACT — do not hand-edit. Source of truth: services/pull-request-template/sections/*.md. Redeployed by .github/workflows/build-pull-request-template.yml -->'
          tmp="$(mktemp)"
          printf '%s\n\n' "$banner" > "$tmp"
          first=1
          for f in services/pull-request-template/sections/*.md; do
            if [ "$first" -eq 0 ]; then printf '\n---\n\n' >> "$tmp"; fi
            cat "$f" >> "$tmp"
            first=0
          done
          mv "$tmp" "$out"

      - name: Redeploy artifact if it drifted from source
        run: |
          set -euo pipefail
          git add -f .github/PULL_REQUEST_TEMPLATE.md
          if git diff --cached --quiet; then
            echo "No drift detected. PULL_REQUEST_TEMPLATE artifact already in production. 🛰️"
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'chore(pull-request-template): redeploy generated artifact to production'
          git push origin HEAD:${{ github.ref_name }}
